Existe no backend, mas o frontend ainda não usa direito:
1. Reset/recuperação de senha
    Backend tem:
    POST /api/auth/forgot-password/
    POST /api/auth/reset-password/
    POST /api/auth/change-password/
    No frontend só vi login, registro, OAuth e verificação de usuário. Não vi tela/fluxo para recuperar senha ou alterar senha.
    Referência: [api/urls.py (line 14)](D:/NdDaniel/Code/42/BidLive/backend/api/urls.py:14)

<!-- 2. Logout real no backend
    Backend tem POST /api/auth/logout/, mas o frontend remove tokens do localStorage localmente.
    Referência: [api/urls.py (line 12)](D:/NdDaniel/Code/42/BidLive/backend/api/urls.py:12) -->

3. Gestão completa de usuários
    Backend permite listar, criar, editar, deletar, hard-delete e banir usuários.
    Frontend usa basicamente listar usuários e banir.
    Faltam telas/ações para criar usuário, editar usuário, soft delete e hard delete.
    Referência: [views.py (line 35)](D:/NdDaniel/Code/42/BidLive/backend/apps/users/api/views.py:35)

4. Gestão completa de roles e permissions
    Backend tem CRUD para:
    /api/roles/
    /api/permissions/
    Frontend mostra permissões no perfil/admin, mas não vi interface para criar/editar/remover roles e permissions.
    Referência: [urls.py (line 6)](D:/NdDaniel/Code/42/BidLive/backend/apps/users/api/urls.py:6)

5. Social/amigos/bloqueios
    Backend tem:
    listar amigos
    enviar pedido
    aceitar/rejeitar pedido
    pedidos enviados/recebidos
    amigos online
    bloquear/desbloquear usuários
    Não vi frontend consumindo esses endpoints.
    Referência: [views.py (line 25)](D:/NdDaniel/Code/42/BidLive/backend/apps/social/api/views.py:25)

<!-- 6. Chat privado e chat de leilão via REST
    Backend tem conversas privadas, mensagens, marcar como lida, apagar mensagem, sala de chat de leilão e envio de mensagem.
    No frontend existe UI de chat, mas parece mais local/visual; não vi métodos em apiService para esses endpoints.
    Referência: [views.py (line 30)](D:/NdDaniel/Code/42/BidLive/backend/apps/chat/api/views.py:30) -->

7. Notificações reais
    Backend tem:
    GET /api/notifications/
    PATCH /api/notifications/{id}/read/
    Frontend ainda carrega/marca notificações via localStorage.
    Referência: [views.py (line 15)](D:/NdDaniel/Code/42/BidLive/backend/apps/notifications/api/views.py:15)

8. Reports reais
    Backend tem criar denúncia, listar denúncias, minhas denúncias, atualizar status e aplicar ação administrativa.
    Frontend mantém reports em localStorage e só muda status localmente.
    Referência: [views.py (line 24)](D:/NdDaniel/Code/42/BidLive/backend/apps/reports/api/views.py:24)

9. Ações extras de leilão
    Backend tem:
    /api/auctions/featured/
    /api/auctions/activities/
    cancelar leilão
    atualizar leilão
    deletar leilão
    Frontend usa listar, detalhes, criar, bid, buy-now e watchlist, mas não vi uso de featured/activities/cancel/update/delete.
    Referência: [auction_views.py (line 57)](D:/NdDaniel/Code/42/BidLive/backend/apps/auctions/api/views/auction_views.py:57)

10. Ações extras de stream
    Backend tem:
    atualizar/deletar stream
    regenerar stream key
    listar viewers
    webhook LiveKit
    Frontend usa criar/start/end/token LiveKit, mas não vi uso de regenerate-key/viewers/update/delete.
    Referência: [__init__.py (line 8)](D:/NdDaniel/Code/42/BidLive/backend/apps/auctions/api/urls/__init__.py:8)

11. Domain/projects
   Backend tem app domain com rota /api/domain/.
   Não vi frontend consumindo esse domínio.
   Referência: [urls.py (line 6)](D:/NdDaniel/Code/42/BidLive/backend/apps/domain/api/urls.py:6)

Resumo curto: o backend está bem mais completo que o frontend. As maiores lacunas de integração hoje parecem ser RBAC completo, notificações reais, reports reais, social/amigos, chat real, recuperação de senha e operações administrativas completas de usuários/leilões/streams.
